"use client";
import React from "react";
// Reuse the existing Home component logic for now.
// The rest of the app (components/libs) is converted to TS; this page will be migrated next.
import Home from "../pages/index";

export default function Page() {
  return <Home />;
}

